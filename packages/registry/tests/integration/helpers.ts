export const MOCK_ROTATION_LOCK_EXPIRATION = 2000;

export const BEFORE_ALL_TIMEOUT = 15000;

export enum Ecosystem {
  OSMDBT = 'osmdbt',
  INGESTION = 'ingestion',
  OSM2PG_RENDERING = 'osm2pg-rendering',
  OSM2PG_QUERY = 'osm2pg-query',
  PLANET_DUMPER = 'planet-dumper',
  RENDERING = 'rendering',
}
